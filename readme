./install_machine.sh							# install the boot2docker machine
./build_image.sh <image_name>					# build a container fron the Dockerfile
./run_container.sh <image_name> <dir_to_copy>	# copy Web at /Users/Shared and run the container
